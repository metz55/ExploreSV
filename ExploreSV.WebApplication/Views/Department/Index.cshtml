@using ExploreSV.BusinessLogic.Utils
@model PaginatedList<ExploreSV.BusinessLogic.DTOs.DepartmentResponse>

@{
    ViewData["Title"] = "Departamentos";
}

<div class="departments-container">
    <div class="departments-header">
        <h1 class="departments-title">
            <i class="fas fa-map-marked-alt"></i> Departamentos
        </h1>
        <a asp-action="Create" class="btn btn-create">
            <i class="fas fa-plus-circle"></i> Nuevo Departamento
        </a>
    </div>

    <div class="table-responsive" style="display: flex; justify-content: center;">
        <table class="table table-premium" style="width: auto; min-width: 500px;">
            <thead>
                <tr>
                    <th style="padding: 10px 20px;">Nombre del Departamento</th>
                    <th style="padding: 10px 20px; text-align: center;">Acción</th>
                </tr>
            </thead>
            <tbody>
                @if (Model.Items.Any())
                {
                    @foreach (var item in Model.Items)
                    {
                        <tr>
                            <td style="padding: 10px 20px;">
                                @Html.DisplayFor(modelItem => item.DepartamentName)
                            </td>
                            <td style="padding: 10px 20px; text-align: center;">
                                <a asp-action="Delete" asp-route-id="@item.DepartmentId" class="btn btn-delete">
                                    <i class="fas fa-trash-alt"></i> Eliminar
                                </a>
                            </td>
                        </tr>
                    }
                }
                else
                {
                    <tr>
                        <td colspan="2" class="no-departments">
                            <i class="fas fa-inbox fa-2x mb-2"></i>
                            <p>No hay departamentos registrados</p>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>

    <!-- Controles de paginación -->
    @if (Model != null && Model.TotalPages > 1)
    {
        <div class="pagination-controls">
            @if (Model.HasPreviousPage)
            {
                <a asp-action="Index"
                   asp-route-pageNumber="@(Model.PageNumber - 1)"
                   asp-route-pageSize="@Model.PageSize"
                   class="pagination-btn">← Anterior</a>
            }
            else
            {
                <span class="pagination-btn disabled" aria-disabled="true">← Anterior</span>
            }
            <span class="pagination-info">Página @Model.PageNumber de @Model.TotalPages</span>
            @if (Model.HasNextPage)
            {
                <a asp-action="Index"
                   asp-route-pageNumber="@(Model.PageNumber + 1)"
                   asp-route-pageSize="@Model.PageSize"
                   class="pagination-btn">Siguiente →</a>
            }
            else
            {
                <span class="pagination-btn disabled" aria-disabled="true">Siguiente →</span>
            }
        </div>
    }
</div>

<style>
    body {
        background: linear-gradient(to bottom, #e3f2fd, #ffffff);
    }

    .departments-container {
        background-color: #ffffff;
        border-radius: 10px;
        box-shadow: 0 6px 18px rgba(0, 0, 0, 0.08);
        padding: 20px 30px !important;
        margin-top: 15px !important;
        max-width: 700px;
        margin-left: auto;
        margin-right: auto;
    }

    .departments-header {
        display: flex;
        justify-content: space-between;
        align-items: center;
        margin-bottom: 10px !important;
        padding-bottom: 8px;
        border-bottom: 2px solid #f1f1f1;
    }

    .departments-title {
        color: #2c3e50;
        font-weight: 700;
        margin: 0;
        font-size: 1.5rem;
    }

    .btn-create {
        background-color: #28a745;
        color: white;
        border: none;
        border-radius: 6px;
        padding: 8px 16px;
        font-weight: 500;
        transition: all 0.3s ease;
        font-size: 0.9rem;
    }

        .btn-create:hover {
            background-color: #218838;
            transform: translateY(-2px);
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            color: white;
            text-decoration: none;
        }

    .table-premium {
        border-collapse: collapse;
        border-radius: 10px;
        overflow: hidden;
        margin: 0 auto;
    }

        .table-premium thead tr {
            background: linear-gradient(135deg, #007bff 0%, #00b4ff 100%);
            color: white;
        }

        .table-premium tbody tr {
            transition: all 0.2s ease;
        }

            .table-premium tbody tr:hover {
                background-color: #f8f9fa;
                transform: translateY(-1px);
                box-shadow: 0 2px 8px rgba(0, 0, 0, 0.05);
            }

    .btn-delete {
        border-radius: 6px;
        padding: 6px 12px;
        font-size: 0.8rem;
        font-weight: 500;
        transition: all 0.2s ease;
        border: none;
        display: inline-flex;
        align-items: center;
        gap: 5px;
        background-color: #dc3545;
        color: white !important;
    }

        .btn-delete:hover {
            transform: translateY(-2px);
            box-shadow: 0 2px 6px rgba(0, 0, 0, 0.1);
            text-decoration: none;
            background-color: #c82333;
            color: white !important;
        }

    .no-departments {
        text-align: center;
        padding: 15px;
        color: #6c757d;
        font-style: italic;
        font-size: 0.9rem;
    }

    /* --- Estilos de paginación --- */
    .pagination-controls {
        margin-top: 50px !important;
        display: flex !important;
        justify-content: center !important;
        align-items: center !important;
        gap: 20px !important;
    }

    .pagination-btn {
        background-color: #007bff !important;
        color: #fff !important;
        padding: 10px 18px !important;
        border-radius: 8px !important;
        font-weight: 600 !important;
        text-decoration: none !important;
        border: none !important;
        display: inline-block !important;
        transition: all 0.3s ease !important;
        box-shadow: 0 3px 8px rgba(0, 0, 0, 0.1) !important;
    }

        .pagination-btn:hover {
            background-color: #0056b3 !important;
            transform: translateY(-2px) !important;
        }

        .pagination-btn.disabled {
            background-color: #e9ecef !important;
            color: #6c757d !important;
            box-shadow: none !important;
            cursor: default !important;
        }

    .pagination-info {
        font-weight: 600 !important;
        color: #2c3e50 !important;
    }
</style>