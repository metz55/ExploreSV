@using ExploreSV.BusinessLogic.Utils
@model PaginatedList<ExploreSV.BusinessLogic.DTOs.UserResponse>

@{
    ViewData["Title"] = "Usuarios";
}

<div class="users-container">
    <div class="users-header">
        <h1 class="users-title">
            <i class="fas fa-users"></i> Usuarios
        </h1>
        <a asp-action="Create" class="btn btn-create">
            <i class="fas fa-plus-circle"></i> Nuevo Usuario
        </a>
    </div>

    <div class="table-responsive" style="display: flex; justify-content: center;">
        <table class="table table-premium" style="width: auto; min-width: 500px;">
            <thead>
                <tr>
                    <th style="padding: 10px 20px;">Usuario</th>
                    <th style="padding: 10px 20px; text-align: center;">Acción</th>
                </tr>
            </thead>
            <tbody>
                @if (Model != null && Model.Items.Any())
                {
                    var currentUserId = int.Parse(User.FindFirst("Id")?.Value);
                    @foreach (var item in Model.Items)
                    {
                        <tr>
                            <td style="padding: 10px 20px;">
                                @Html.DisplayFor(modelItem => item.UserName)
                            </td>
                            <td style="padding: 10px 20px; text-align: center;">
                                @if (item.UserId == currentUserId)
                                {
                                    <a class="btn btn-delete btn-delete-disabled" disabled>
                                        <i class="fas fa-trash-alt"></i> Eliminar
                                    </a>
                                }
                                else
                                {
                                    <a asp-action="Delete" asp-route-id="@item.UserId" class="btn btn-delete">
                                        <i class="fas fa-trash-alt"></i> Eliminar
                                    </a>
                                }
                            </td>
                        </tr>
                    }
                }
                else
                {
                    <tr>
                        <td colspan="2" class="no-users">
                            <i class="fas fa-inbox fa-2x mb-2"></i>
                            <p>No hay usuarios registrados</p>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>

    <!-- Controles de paginación -->
    @if (Model != null && Model.TotalPages > 1)
    {
        <div class="pagination-controls">
            @if (Model.HasPreviousPage)
            {
                <a asp-action="Index"
                   asp-route-pageNumber="@(Model.PageNumber - 1)"
                   asp-route-pageSize="@Model.PageSize"
                   class="pagination-btn">← Anterior</a>
            }
            else
            {
                <span class="pagination-btn disabled" aria-disabled="true" style="opacity:0.5; cursor:default;">← Anterior</span>
            }
            <span class="pagination-info">Página @Model.PageNumber de @Model.TotalPages</span>
            @if (Model.HasNextPage)
            {
                <a asp-action="Index"
                   asp-route-pageNumber="@(Model.PageNumber + 1)"
                   asp-route-pageSize="@Model.PageSize"
                   class="pagination-btn">Siguiente →</a>
            }
            else
            {
                <span class="pagination-btn disabled" aria-disabled="true" style="opacity:0.5; cursor:default;">Siguiente →</span>
            }
        </div>
    }
</div>

<style>
    body {
        background: linear-gradient(to bottom, #e3f2fd, #ffffff);
    }

    .users-container {
        background-color: #ffffff;
        border-radius: 10px;
        box-shadow: 0 6px 18px rgba(0, 0, 0, 0.08);
        padding: 20px 30px !important;
        margin-top: 15px !important;
        max-width: 700px;
        margin-left: auto;
        margin-right: auto;
    }

    .users-header {
        display: flex;
        justify-content: space-between;
        align-items: center;
        margin-bottom: 10px !important;
        padding-bottom: 8px;
        border-bottom: 2px solid #f1f1f1;
    }

    .users-title {
        color: #2c3e50;
        font-weight: 700;
        margin: 0;
        font-size: 1.5rem;
    }

    .btn-create {
        background-color: #28a745;
        color: white;
        border: none;
        border-radius: 6px;
        padding: 8px 16px;
        font-weight: 500;
        transition: all 0.3s ease;
        font-size: 0.9rem;
    }

        .btn-create:hover {
            background-color: #218838;
            transform: translateY(-2px);
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            color: white;
            text-decoration: none;
        }

    .table-premium {
        border-collapse: collapse;
        border-radius: 10px;
        overflow: hidden;
        margin: 0 auto;
    }

        .table-premium thead tr {
            background: linear-gradient(135deg, #007bff 0%, #00b4ff 100%);
            color: white;
        }

        .table-premium tbody tr {
            transition: all 0.2s ease;
        }

            .table-premium tbody tr:hover {
                background-color: #f8f9fa;
                transform: translateY(-1px);
                box-shadow: 0 2px 8px rgba(0, 0, 0, 0.05);
            }

    .btn-delete {
        border-radius: 6px;
        padding: 6px 12px;
        font-size: 0.8rem;
        font-weight: 500;
        transition: all 0.2s ease;
        border: none;
        display: inline-flex;
        align-items: center;
        gap: 5px;
        background-color: #dc3545;
        color: white !important;
    }

        .btn-delete:hover {
            transform: translateY(-2px);
            box-shadow: 0 2px 6px rgba(0, 0, 0, 0.1);
            text-decoration: none;
            background-color: #c82333;
            color: white !important;
        }

    .no-users {
        text-align: center;
        padding: 15px;
        color: #6c757d;
        font-style: italic;
        font-size: 0.9rem;
    }

    .btn-delete-disabled {
        background-color: #adb5bd !important;
        cursor: not-allowed !important;
        opacity: 0.8;
        transform: none !important;
        box-shadow: none !important;
    }

        .btn-delete-disabled:hover {
            background-color: #adb5bd !important;
            transform: none !important;
            box-shadow: none !important;
        }

    /* --- Estilos de paginación --- */
    .pagination-controls {
        margin-top: 50px !important;
        display: flex !important;
        justify-content: center !important;
        align-items: center !important;
        gap: 20px !important;
    }

    .pagination-btn {
        background-color: #007bff !important;
        color: #fff !important;
        padding: 10px 18px !important;
        border-radius: 8px !important;
        font-weight: 600 !important;
        text-decoration: none !important;
        border: none !important;
        display: inline-block !important;
        transition: all 0.3s ease !important;
        box-shadow: 0 3px 8px rgba(0, 0, 0, 0.1) !important;
    }

        .pagination-btn:hover {
            background-color: #0056b3 !important;
            transform: translateY(-2px) !important;
        }

        .pagination-btn.disabled {
            background-color: #e9ecef !important;
            color: #6c757d !important;
            box-shadow: none !important;
            cursor: default !important;
        }

    .pagination-info {
        font-weight: 600 !important;
        color: #2c3e50 !important;
    }

</style>